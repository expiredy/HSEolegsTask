#include <iostream>

int main() {
    int totalIterations, totalNumbers, number;

    std::cin >> totalIterations;

    for (int totalIndex = 0; totalIndex < totalIterations; totalIndex++){
        std::cin >> totalNumbers;
        for(int numberIndex = 0; numberIndex << totalNumbers; numberIndex++){
            std::cin >> number;

        }
    }

    return 0;
}
